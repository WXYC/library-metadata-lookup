"""Seed / revoke / list CLI for lml_cache.api_keys (LML per-consumer API keys plan).

Mints a per-consumer bearer token (shown once, never logged), revokes a row
by id, or lists operational metadata (never key_hash or plaintext). See
``docs/plans/lml-per-consumer-api-keys.md`` and ``docs/scripts.md``.
"""
